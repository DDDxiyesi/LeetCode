public class Solution {
    public int findMin(int[] nums) {
        int max = nums[0];
        for(int i : nums){
            if(i > max){
                max = i;
            }
            if(i < max)
                return i;
        }
        return nums[0];
    }
}
